# ansible/inventory/hosts.yml
# Inventory for Medusa DTC Storefront POC Docker Hosts

all:
  children:
    poc_docker_hosts:
      hosts:
        localhost:
          ansible_connection: local
          ansible_python_interpreter: "{{ ansible_playbook_python }}"
