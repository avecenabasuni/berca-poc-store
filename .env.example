# Datadog Agent Configuration
DD_API_KEY=
DD_SITE=datadoghq.com

# Medusa Storefront Publishable API Key
MEDUSA_PUBLISHABLE_KEY=

# Public storefront URL opened by users. Browser API calls are proxied through
# this HTTPS origin; the Medusa container is reached only over the Docker network.
STOREFRONT_PUBLIC_URL=https://store.example.invalid

# Calibrated only on the 16 GiB POC application VM. The Compose service accepts
# the fixed bounded values 512M, 768M, 1024M, 1280M, or 1536M. Keep the limit
# exactly 256 MiB above the selected allocation.
MEMORY_PRESSURE_BYTES=1024M
MEMORY_PRESSURE_LIMIT=1280M
